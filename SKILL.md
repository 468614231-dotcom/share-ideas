---
name: cyberpunk-presentation
description: 生成专业风格、单文件 HTML 演讲汇报/演示页面。白/浅灰底、无黑色，内置 3 套差异化设计皮肤（科技蓝·现代圆润 / 咨询金·克制优雅 / 墨绿金·几何精确）按 T 键切换；浅色底、强调色 ≤3、含面积图/环形进度/图标要点/描边封面等视觉锚点组件与 count-up/滑动/倾斜动效。适配产品发布、方案评审、高管述职、金融年报。当用户说"做个汇报页/演示页/路演 HTML/网页版 PPT/述职报告页"时使用。
---

# Pro Presentation（专业汇报页生成器）

把用户内容转成一份可直接打开、可上台演示的单文件 HTML 汇报页：浅色专业底、三套差异化设计皮肤、≤3 强调色、视觉锚点组件、强标题层级、总分总结构、全套交互。

## 何时使用

- 用户要"演讲/汇报/述职/路演/答辩/方案评审"的 **HTML 演示页**
- 交付形态是 HTML 文件（不是飞书幻灯片、不是 .pptx）

## 三套设计皮肤（差异化，不只是换色；模板内按 T 键实时切换对比）

| 皮肤 | body 类 | 设计语言 | 色板（primary/accent/highlight） | 适用场景 |
|---|---|---|---|---|
| **科技蓝**（默认） | `theme-tech` | 现代圆润：大圆角 + 毛玻璃卡片 + 蓝调柔和阴影 | 蓝 #2563EB / 青绿 #0E9F8A / 琥珀 #D97706 | 产品发布、技术汇报、增长路演 |
| **咨询金** | `theme-consult` | 克制优雅：细线小圆角 + 衬线标题 + 大留白 | 金 #B08A2E / 藏蓝 #2F5B96 / 深金棕 #8C6D1F | 方案评审、商业分析、高管述职 |
| **墨绿金** | `theme-finance` | 几何精确：直角 + 细网格底 + 高数据密度 | 墨绿 #2F7D5C / 金 #A17E24 / 银灰 #3F4A44 | 年报、财报、投资汇报 |

选定后把 `<body>` 的 class 固定为对应值（如 `class="theme-consult"`），或保留默认按 T 切换。

## 快速开始

1. 读取 `assets/template.html`，复制为输出文件（建议中文名，如 `述职报告.html`）
2. 选皮肤：改 `<body class="theme-xxx">`；换封面版式：`cover` 加 `left` 或 `split` 类
3. 按「总分总」骨架逐页替换 `<section class="slide">` 内容，页数 5–8 页
4. 遵循「内容规范」与「设计规范」
5. 用浏览器打开验证：层级、配色、无溢出、动画与交互正常
6. 交付：`present_files` 交付 HTML 文件，快捷键写在交付说明里（页面不显示）

## 总分总结构（硬性）

- **总起**（1–2 页）：封面一句话结论 → 概览页核心数字
- **分述**（主体 3–5 页）：一页一个主题，指标 / 项目 / 复盘各自独立成页
- **总结**（收尾 1 页）：下一步计划 + 一句话定性（quote）+ 开放提问
- 每页内部也"结论先行"：lead 一句话结论 → 组件佐证 → 口径小字

## 内容规范（硬性）

- 一页只讲一个主题；要点 ≤ 5 条；单条 ≤ 22 字；能用数字不用形容词
- **删除赘述**：删口头语、铺垫、重复解释；每句只留主谓宾 + 一个数据
- 标题层级（模板已定义，勿自造）：`.kicker` 页签 → `h1` 页标题（主题色下划线）→ `h2` 分节 → `h3` 要点 → 正文
- 每页 `<section class="slide">` 写 `data-title` 与 `data-note`（演讲备注，N 键显示）；封面另写 `data-year`（描边年份锚点）
- 数据页加一行小字口径说明

## 设计规范（硬性）

- **底色**：白 / 浅灰，禁止黑色底与大面积深色块；正文深色、次级文字中灰
- 每主题强调色 ≤ 3 种：`--primary` / `--accent` / `--highlight`；`--danger` 仅限风险/负值语义
- **皮肤 token 随主题联动**：圆角（`--radius-*`）、阴影（`--shadow-*`）、标题字体（`--font-display`）、玻璃强度（`--card-glass/--card-blur`）、背景纹样（`--bg-pat`）——改主题自动换设计语言
- **视觉锚点**：每页至少一个图形组件（面积图/环形/图标/描边字/时间线），避免纯文字页
- **元素精简**：页面只保留 顶栏、进度条、圆点、◀▶、备注；不加页脚描述、霓虹辉光、闪烁动效
- 动效克制：滑动进场 + 数字滚动 + 卡片倾斜/浮起，全部可被 `prefers-reduced-motion` 关闭

## 内置交互（默认开启）

- 翻页：←/→/↑/↓ / 空格 / PageUp/PageDown / Home/End / 滚轮 / 触屏滑动 / 点击左右区域 / ◀▶；数字键跳页
- 功能：T 皮肤切换（右上角显示皮肤名）· F 全屏 · N 演讲备注 · 顶部进度条 · 页码 · 圆点导航
- 动效：滑动进场、进场错峰（.reveal）、数字滚动（.num[data-count]）、面积图描线、环形进度、卡片倾斜跟随、打字机（.tw）

## 组件速查（模板内均有示例）

| 组件 | 用法 | 用途 |
|---|---|---|
| 面板（角标+玻璃） | `.panel` | 要点容器 |
| 卡片（玻璃+倾斜） | `.card` | 并列要点块 |
| 大数字（自动滚动） | `.stat` > `.num`（`data-count` `data-suffix`） | 核心指标 |
| 面积图 | `.chart-area` + SVG（渐变填充+描线动画） | 趋势展示 |
| 环形进度 | `.ring`（style=`--pct:72`，内数字 data-count） | 完成率/占比 |
| 图标要点行 | `.card.icon-row` > `.ico` + div | 板块要点（带图形锚点） |
| 时间线 | `.steps` > `.step` > `.node` | 阶段/里程碑 |
| 分栏 | `.grid-2` / `.grid-3` / `.grid-t2` | 双列/三列/主次分栏 |
| 要点列表 | `.list` | 方块符号列表 |
| 标签 | `.tag` / `.tag.p` / `.tag.h` | 方案名、状态 |
| 金句 | `.quote` | 收尾点睛 |
| 风险行 | `.risk` + `.risk-lv lv-high/mid/low` | 风险与应对 |
| 封面版式 | `.cover`（默认居中）/ `.cover.left` / `.cover.split` | 三种封面可选 |

## 详细参考

- `references/design.md` — 皮肤 token 体系、三套皮肤色板、总分总骨架、页面类型、简洁度检查清单
- `references/interactions.md` — 交互清单、JS 配置项、皮肤定制、常见问题

## 验证与交付

- 打开输出文件，逐页检查：浅色底、皮肤统一、主题色 ≤3、视觉锚点存在、无文字溢出、动画/翻页/备注/皮肤切换正常
- 交付说明写清：文件路径、打开方式（浏览器）、快捷键（←→ 翻页 · T 皮肤 · F 全屏 · N 备注）
